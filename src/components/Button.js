import React from 'react'

const Button = (props) => {
    const {btnLabel, classname} = props;

    return (
        // <button className={classname}>{btnLabel}</button>
    )
}

export default Button;